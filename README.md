# Dining_Philosophers_Problem
A multi-threaded Java Swing application simulating Dijkstra's classic Dining Philosophers problem. Implements concurrency controls using Semaphores and prevents circular-wait deadlocks via asymmetric fork acquisition rules.
